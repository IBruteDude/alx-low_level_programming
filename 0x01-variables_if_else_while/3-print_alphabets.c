#include "stdio.h"

/**
 * main - Entry point to project
 * Return: returns exit status
 */
int main(void)
{
	/* like a fucking chad */
	for (char c = 'a'; c <= 'z'; putchar(c++))
		;
	/* and again LOL */
	for (char c = 'A'; c <= 'Z'; putchar(c++))
		;
	putchar('\n');
	return (0);
}
