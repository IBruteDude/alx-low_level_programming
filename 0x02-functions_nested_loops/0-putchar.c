#include "main.h"

/**
 * main - Entry point to project
 * Return: returns exit status
 */
int main(void)
{
	char s[] = "_putchar";
	int i = 0; size = 8;
	for (;i < size; _putchar(s[i++]))
		;
	_putchar('\n');
	return (0);
}
