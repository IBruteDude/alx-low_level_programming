#include <string.h>
#include <stdio.h>
/**
 * _strstr - finds a substring in a string
 * @haystack: main string
 * @needle: substring
 * Return: address of substring if found, NULL in not
 */
char *_strstr(char *haystack, char *needle)
{
	char *s = haystack;
	s = needle;
	
	return (s);
}
